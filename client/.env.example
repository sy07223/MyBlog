# 复制为 client/.env 或 client/.env.local（可选）

# 生产构建时：填后端 API 根地址，例如 https://api.example.com
# 留空则在开发时走 Vite 代理 /api → localhost:3001
# VITE_API_BASE=

# 可选：覆盖默认背景（见 client/src/config/blogTheme.js）
# VITE_BLOG_BG_URL=/blog-bg.svg
